{
    "githubToken": "$GITHUB_TOKEN",
    "server": {
        "cache": {
            "engine": "catbox-redis",
            "host": "redis"
        }
    },
    "redis": {
        "host": "${self.server.cache.host}"
    }
}
